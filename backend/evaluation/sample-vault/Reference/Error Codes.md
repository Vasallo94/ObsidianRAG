# Error codes

## ORG-429

`ORG-429` means the organization exceeded its request rate. Wait for the `Retry-After` interval before retrying.

## AUTH-401

`AUTH-401` means the supplied credential is missing, expired, or invalid.
